import Nav from "./nav"
import Head from "next/head";
const Layout = (props) => {
    return(
        <>
        <Head>
            <title>Portafolio Web</title>
        </Head>
        <Nav/>
        </>
    )
}

export default Layout;